# backend/app/modules/delivery/router.py
# Paquete "Gestión Delivery" — Envios, Agencias
# Base para siguientes ciclos (CU de delivery aún no definidos).
from fastapi import APIRouter

router = APIRouter(tags=["Delivery"])
