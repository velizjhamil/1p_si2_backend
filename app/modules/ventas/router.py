# backend/app/modules/ventas/router.py
# Paquete "Gestión Venta" — Carrito, Ventas, Pagos
# Base para siguientes ciclos (CU de venta aún no definidos).
from fastapi import APIRouter

router = APIRouter(tags=["Ventas"])
