# backend/app/modules/inventario/router.py
# Paquete "Gestión Inventario" — Productos, Stock, Kardex, ModelosAR
# Base para siguientes ciclos (CU de inventario aún no definidos).
from fastapi import APIRouter

router = APIRouter(tags=["Inventario"])
