# backend/app/modules/descuentos/router.py
# Stub: los endpoints viven en app/api/v1/endpoints/descuentos.py
# para mantener el mismo patron que el resto del proyecto.
from fastapi import APIRouter

router = APIRouter(tags=["Descuentos"])
